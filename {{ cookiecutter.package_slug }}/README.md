# {{ cookiecutter.package_description }} ({{ cookiecutter.package_slug }})

#### Requirements

- Python 3.7+
- Python packages:
  - numpy
  - pandas
  - networkx
  - scipy
  - scikit-image
  - matplotlib
See full list in the [requirements file](requirements.txt).

#### Install

```bash
pip install git+https://github.com/afrendeiro/{{ cookiecutter.package_slug }}.git
```
